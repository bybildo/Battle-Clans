<Page x:Class="Battle_Clans.Windows.BattlePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      Title="BattlePage" Height="800" Width="1400"
      Background="#F0F0F0">

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <DropShadowEffect x:Key="ShadowEffect" BlurRadius="15" ShadowDepth="3" Opacity="0.1" Color="Black"/>

        <Style x:Key="UnitCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Width" Value="140"/>
            <Setter Property="Effect" Value="{StaticResource ShadowEffect}"/>
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="30"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="0,10,0,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <DataTemplate x:Key="PlayerUnitTemplate">
            <Border Style="{StaticResource UnitCardStyle}" BorderBrush="#4CAF50" BorderThickness="0,0,0,3" FlowDirection="LeftToRight">
                <StackPanel>
                    <TextBlock Text="{Binding Name, Mode=OneWay}" FontWeight="Bold" FontSize="14" HorizontalAlignment="Center" Margin="0,0,0,5"/>

                    <Rectangle Height="60" Width="60" Fill="#81C784" RadiusX="30" RadiusY="30" Margin="5"/>

                    <Grid Margin="0,5,0,2">
                        <ProgressBar Value="{Binding Health, Mode=OneWay}" Maximum="{Binding MaxHealth, Mode=OneWay}" Height="8" Foreground="#E53935" Background="#FFEBEE" BorderThickness="0"/>
                        <TextBlock Text="HP" FontSize="8" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White" FontWeight="Bold"/>
                    </Grid>

                    <Grid Margin="0,0,0,5">
                        <ProgressBar Value="{Binding Armor, Mode=OneWay}" Maximum="100" Height="6" Foreground="#1E88E5" Background="#E3F2FD" BorderThickness="0"/>
                    </Grid>

                    <Button Content="⚔️ АТАКА" 
                            Background="#4CAF50"
                            Style="{StaticResource ActionButtonStyle}"
                            Tag="{Binding Mode=OneWay}" 
                            Click="OnPlayerUnitClick"/>
                </StackPanel>
            </Border>
        </DataTemplate>

        <DataTemplate x:Key="EnemyUnitTemplate">
            <Border Style="{StaticResource UnitCardStyle}" BorderBrush="#E53935" BorderThickness="0,0,0,3">
                <StackPanel>
                    <TextBlock Text="{Binding Name, Mode=OneWay}" FontWeight="Bold" FontSize="14" HorizontalAlignment="Center" Margin="0,0,0,5"/>

                    <Rectangle Height="60" Width="60" Fill="#E57373" RadiusX="30" RadiusY="30" Margin="5"/>

                    <Grid Margin="0,5,0,2">
                        <ProgressBar Value="{Binding Health, Mode=OneWay}" Maximum="{Binding MaxHealth, Mode=OneWay}" Height="8" Foreground="#E53935" Background="#FFEBEE" BorderThickness="0"/>
                        <TextBlock Text="HP" FontSize="8" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White" FontWeight="Bold"/>
                    </Grid>

                    <Grid Margin="0,0,0,5">
                        <ProgressBar Value="{Binding Armor, Mode=OneWay}" Maximum="100" Height="6" Foreground="#1E88E5" Background="#E3F2FD" BorderThickness="0"/>
                    </Grid>

                    <Button x:Name="TargetButton"
                            Content="🎯 ЦІЛЬ" 
                            Background="#FF5722"
                            Style="{StaticResource ActionButtonStyle}"
                            Visibility="Collapsed" 
                            Tag="{Binding Mode=OneWay}"
                            Click="OnEnemyUnitClick"/>
                </StackPanel>
            </Border>
        </DataTemplate>
    </Page.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,20">
            <TextBlock Text="ПОЛЕ БОЮ" FontSize="40" FontWeight="Heavy" Foreground="#333" HorizontalAlignment="Center"/>
            <TextBlock Text="БИТВА ПОЧАЛАСЯ" FontSize="16" FontWeight="SemiBold" Foreground="Gray" HorizontalAlignment="Center"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ItemsControl Grid.Column="0" 
                          ItemsSource="{Binding PlayerTeam, Mode=OneWay}" 
                          ItemTemplate="{StaticResource PlayerUnitTemplate}"
                          FlowDirection="RightToLeft">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Rows="1" HorizontalAlignment="Left"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>

            <Border Grid.Column="1" Background="#D32F2F" CornerRadius="25" Width="50" Height="50" VerticalAlignment="Center" Margin="20">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" Color="#D32F2F" Opacity="0.5"/>
                </Border.Effect>
                <TextBlock Text="VS" Foreground="White" FontWeight="Black" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>

            <ItemsControl Grid.Column="2" 
                          ItemsSource="{Binding EnemyTeam, Mode=OneWay}" 
                          ItemTemplate="{StaticResource EnemyUnitTemplate}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Rows="1" HorizontalAlignment="Left"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </Grid>

        <TextBlock Grid.Row="2" Text="Оберіть свого воїна, щоб атакувати" FontSize="14" Foreground="#888" HorizontalAlignment="Center" Margin="0,20,0,0"/>
    </Grid>
</Page>